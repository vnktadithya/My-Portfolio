# My Portfolio 🚀

This is your interactive portfolio built with **Next.js + Tailwind CSS + Framer Motion**.

## ⚙️ Setup Instructions
1. Extract the ZIP file.
2. Run `npm install` to install dependencies.
3. Run `npm run dev` to start the development server at `http://localhost:3000`.
4. Update `pages/index.js` and `components/` files with your personal info.
5. Deploy easily on [Vercel](https://vercel.com/) — just connect your GitHub repo.

---
💡 **Tip:** Modify or add new sections (Projects, Skills, Contact) in the `pages/` or `components/` folder.
